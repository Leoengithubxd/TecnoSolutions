
@{
    ViewBag.Title = "Home";
    Layout = "~/Views/Shared/_LayoutCliente.cshtml";
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Home - TechnoSolutions</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

</head>


<body>
    <div class="container-fluid">
        <div class="container text-center">
            <div class="row align-items-start">
                <div class="col">
                    <a href="@Url.Action("SelectProducts", "Product")" class="btn img-button">
                        <img src="~/Content/Image/informatica.png" alt="Botón de imagen" class="img-fluid ">
                        <span class="img-button-text">Cotiza tu servicio</span>


                    </a>
                </div>
                <div class="col">
                    <a href="@Url.Action("SelectProducts", "Product")" class="btn img-button">
                        <img src="~/Content/Image/it-systems_10365705 (1).png" alt="Botón de imagen" class="img-fluid ">
                        <span class="img-button-text">Nuestros productos</span>

                    </a>
                </div>
                <div class="col">
                    <a href="@Url.Action("SelectProducts", "Product")" class="btn img-button">
                        <img src="~/Content/Image/financial-plan_8733607.png" alt="Botón de imagen" class="img-fluid ">
                        <span class="img-button-text">Revisa tu cotización</span>
                     
                    </a>
                </div>
            </div>
            <div>

            </div>
        </div>
    </div>
</body>
</html>


<style>

    full-width-image {
        width: 100%; /* La imagen ocupará todo el ancho del contenedor */
        height: auto; /* Mantiene la altura proporcional original */
        object-fit: cover; /* Ajusta la imagen sin deformarla */
        opacity: 0.8;
    }

    .image-container {
        position: relative; /* Necesario para posicionar el botón dentro de la imagen */
        width: 100%; /* El contenedor tiene el mismo ancho que la pantalla */
        overflow: hidden; /* Oculta cualquier parte de la imagen que sobresalga del contenedor */
    }

    .button-container {
        position: absolute;
        bottom: 10px; /* Coloca el botón 10px por encima de la parte inferior de la imagen */
        left: 50%; /* Centra el botón horizontalmente */
        transform: translateX(-50%); /* Asegura que el botón esté completamente centrado */
       
    }

    .img-button-text {
        font-size: 30px; /* Tamaño del texto */
        color: #091324; /* Color del texto */
        font-family: 'Arial', sans-serif; /* Fuente personalizada */
        display: block; /* Hace que el texto esté en su propia línea */
     
    }

    .img-button img {
        width: 80%; /* Se ajusta al 80% del contenedor */
        max-width: 300px; /* Máximo tamaño en pantallas grandes */
        height: auto; /* Mantiene la proporción */
        object-fit: contain;
        margin-bottom: 5px;
      
    }

    .img-button {
        background-color: whitesmoke;
        border: ridge;
    }
 



    .text-principal {
        color: whitesmoke;
    }
</style>

